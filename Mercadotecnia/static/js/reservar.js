document.getElementById("reservaForm").addEventListener("submit", function (e) {
    const nombre = document.getElementById("nombre").value;
    alert("¡Gracias por reservar, " + nombre + "! Te contactaremos pronto.");
});
